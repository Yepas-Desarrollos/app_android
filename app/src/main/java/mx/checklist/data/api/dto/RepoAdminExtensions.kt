package mx.checklist.data.api.dto

// Extension functions para el Repo que agregan funcionalidad admin
// Esto debería ser integrado en el archivo Repo.kt principal

// NOTA: Este archivo contiene las funciones que deben agregarse al Repo.kt
// Ya que no podemos leer/editar el archivo Repo.kt desde aquí

/*
suspend fun Repo.adminGetTemplates(): List<AdminTemplateDto> {
    return api.adminGetTemplates()
}

suspend fun Repo.adminCreateTemplate(request: CreateTemplateDto): CreateTemplateRes {
    return api.adminCreateTemplate(request)
}

suspend fun Repo.adminGetTemplate(templateId: Long): AdminTemplateDto {
    return api.adminGetTemplate(templateId)
}

suspend fun Repo.adminUpdateTemplate(templateId: Long, request: UpdateTemplateDto): AdminTemplateDto {
    return api.adminUpdateTemplate(templateId, request)
}

suspend fun Repo.adminDeleteTemplate(templateId: Long): DeleteRes {
    return api.adminDeleteTemplate(templateId)
}

suspend fun Repo.adminCreateItem(templateId: Long, request: CreateItemTemplateDto): CreateItemTemplateRes {
    return api.adminCreateItem(templateId, request)
}

suspend fun Repo.adminUpdateItem(templateId: Long, itemId: Long, request: UpdateItemTemplateDto): ItemTemplateDto {
    return api.adminUpdateItem(templateId, itemId, request)
}

suspend fun Repo.adminDeleteItem(templateId: Long, itemId: Long): DeleteRes {
    return api.adminDeleteItem(templateId, itemId)
}
*/

// INSTRUCCIONES:
// 1. Copiar estas funciones al archivo Repo.kt
// 2. Asegurarse de que el import Api esté disponible
// 3. Verificar que todos los DTOs estén importados

class RepoAdminExtensions {
    // Esta clase existe solo para documentar las extensiones necesarias
}